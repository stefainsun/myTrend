eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
feign.hystrix.enabled: true
spring:
  application:
    name: trading-backtest-service
  zipkin:
    base-url: http://localhost:9411
management:
  endpoints:
    web:
      exposure:
        include: "*"
      cors:
        allowed-origins: "*"
        allowed-methods: "*"


